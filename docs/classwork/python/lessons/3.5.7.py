# exactly the same as before the only thing that's changed is this comment
length = float(input("length? "))
width = float(input("width? "))

print("area: "+str(length*width))
print("perimeter: "+str((2*(length+width))))